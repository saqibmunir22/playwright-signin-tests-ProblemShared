{
  "name": "saucedemo-playwright-ts",
  "version": "1.0.0",
  "description": "Playwright + TypeScript UI automation for Sauce Labs demo app",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "@types/node": "^20.12.0",
    "typescript": "^5.4.0"
  }
}
